import React from 'react';
import { Scope } from './types';
export declare const useBridge: (scope?: string | number | symbol | undefined) => [any, any];
export declare const Bridge: React.FC<{
    value: ReturnType<typeof useBridge>;
    scope?: Scope;
}>;
